{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Enable or disable the new authorization event. 
  // Username/password is test/test.
  // For endpoint routing one can also use app.MapTus("route", <config>).RequireAuthorization();
  // to use the built in authorization in ASP.NET Core.
  "EnableOnAuthorize": false
}
